-- lab1_select.sql: Basics
-- 1) List all student full names (first + last), ordered by last name.
-- 2) Show all courses ordered by course_code ascending.
-- 3) Return the 2 newest students by enrolled_on (ties allowed).
-- 4) Show distinct email domains from students (hint: split on '@').

-- Starter examples:
-- SELECT * FROM lab.students;
-- SELECT * FROM lab.courses;
